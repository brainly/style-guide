const rules = [
  {
    pattern: '<b>Should</b> respect <code>prefers-reduced-motion</code>.',
    comment: `<code>prefers-reduced-motion</code> system setting is respected. Alternate animations are used when the user has requested that the system minimize the amount of animation or motion it uses.`,
    status: 'DONE',
    tests: 'N/A',
  },
];

export default rules;
