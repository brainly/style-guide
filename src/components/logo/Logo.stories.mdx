import {ArgsTable, Meta, Story, Canvas} from '@storybook/addon-docs';
import {StoryVariant} from '../../_docs/utils';
import {Preview, Table} from '../../_docs/blocks/';

import Logo, {TYPE} from './Logo';

<Meta title="Components/Logo" component={Logo} />

# Logo

Table of contents:

- [Props](#props)
- [Stories](#stories)
- [Accesibility](#accessibility)

## Props

<ArgsTable of={Logo} />

## Stories

### Default

<Canvas>
  <Story name="Default">{args => <Logo {...args} />}</Story>
</Canvas>

### Types

<Canvas>
  <Story name="Types">
    {args => (
      <div>
        {Object.values(TYPE).map(type => (
          <StoryVariant label={`type - ${type}`} key={type}>
            <Logo {...args} type={type} />
          </StoryVariant>
        ))}
      </div>
    )}
  </Story>
</Canvas>

### Product logos

<Canvas>
  <Story name="ProductLogos">
    {args => (
      <div>
        {Object.values(TYPE)
          .filter(type => type.includes('logo'))
          .map(type => (
            <StoryVariant label={`type - ${type}`} key={type}>
              <Logo {...args} type={type} />
            </StoryVariant>
          ))}
      </div>
    )}
  </Story>
</Canvas>

# Accessibility

## Rules

<Table
  headings={['Pattern', 'Comment', 'Status']}
  rows={[
    {
      pattern: `<strong>Should</strong> have an accessible alt text`,
      comment: `Can be named by setting a value for <code>alt</code> prop (defaults
            to logo type).`,
      status: `Implementation: DONE
            <br />
            Tests: DONE`,
    },
  ]}
/>

## Usage

<br />

## Code examples

- with an `alt` text

<!-- prettier-ignore -->
```jsx
<Logo
  alt="Brainly Plus Extra Special"
/>
```
