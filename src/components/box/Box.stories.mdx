import {ArgsTable, Meta, Story, Canvas} from '@storybook/addon-docs';
import {StoryVariant} from '../../_docs/utils';
import {Preview} from '../../_docs/blocks/';

import Box, {PADDING, COLOR} from './Box';
import BoxA11y from './stories/Box.a11y.mdx';

<Meta
  title="Layout/Box"
  component={Box}
  argTypes={{
    children: {
      control: {
        disable: true,
      },
    },
    className: {
      control: {
        disable: true,
      },
    },
    border: {
      control: {
        type: 'boolean',
      },
    },
    padding: {
      control: {
        type: 'select',
        options: PADDING,
      },
    },
  }}
  args={{
    children: 'Text inside Box',
  }}
/>

# Box

Container for grouping elements. It provides padding, background color, border and shadow.

- [Stories](#stories)
- [Accesibility](#box-accessibility)

## Overview

<Canvas>
  <Story name="Default">{args => <Box {...args} />}</Story>
</Canvas>

<ArgsTable story="Default" />

## Stories

### Colors

<Canvas>
  <Story name="Colors">
    {args => (
      <div>
        {Object.values(COLOR).map(color => (
          <StoryVariant key={color} label={`color - ${color}`}>
            <Box {...args} color={color} />
          </StoryVariant>
        ))}
      </div>
    )}
  </Story>
</Canvas>

### Paddings

<Canvas>
  <Story name="Paddings">
    {args => (
      <div>
        {Object.values(PADDING).map(padding => (
          <StoryVariant label={`padding - ${padding}`} key={padding}>
            <Box {...args} color="blue-40" padding={padding} />
          </StoryVariant>
        ))}
      </div>
    )}
  </Story>
</Canvas>

### Borders

<Canvas>
  <Story name="Borders">
    {args => (
      <div>
        {Object.values(COLOR).map(color => (
          <StoryVariant label={`border color - ${color}`} key={color}>
            {color === COLOR.white ? (
              <div className="sg-story-variant-dark-box">
                <Box
                  {...args}
                  color="blue-40"
                  border
                  borderColor={COLOR[color]}
                />
              </div>
            ) : (
              <Box {...args} border borderColor={color} />
            )}
          </StoryVariant>
        ))}
      </div>
    )}
  </Story>
</Canvas>

### Shadow

<Canvas>
  <Story name="Shadow">
    {args => <Box {...args} color="blue-40" shadow />}
  </Story>
</Canvas>

<BoxA11y />
