// // @codegen
// const {TEXT_COLOR} = require('./Text');
// const screenPrefixes = ['', 'md:', 'lg:', 'xl:'];
// const fs = require('fs');
// const path = require('path');

// let tailwindClasses = '';

// screenPrefixes.forEach(screenPrefix => {
//     Object.values().forEach(color => {
//         tailwindClasses += `sg-headline--${color}`;
//     });
// });

// module.exports = tailwindClasses;

sources

component prop types - ast

generateResponsiveClassNames - codegen

tailwind config - codegen

