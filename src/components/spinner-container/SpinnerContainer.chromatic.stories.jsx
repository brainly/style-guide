import * as SpinnerContainer from './SpinnerContainer.stories.jsx';
import {mergeStories} from '../../chromatic/utils';

export const Default = mergeStories(SpinnerContainer);

export default SpinnerContainer.default;
