import {ArgsTable, Meta, Story, Canvas} from '@storybook/addon-docs';
import {StoryVariant, StoryVariantTable} from '../../docs/utils';
import PageHeader from 'blocks/PageHeader';

import Text from '../text/Text';
import Button from '../buttons/Button';
import Flex from '../flex/Flex';
import Checkbox from '../form-elements/checkbox/Checkbox';
import Radio from '../form-elements/radio/Radio';
import Icon from '../icons/Icon';
import Tooltip from './Tooltip';
const TOOLTIP_PLACEMENTS = [
  'top',
  'top-start',
  'top-end',
  'bottom',
  'bottom-start',
  'bottom-end',
  'left',
  'right',
];

<Meta title="Components/Tooltip" component={Tooltip} />

<PageHeader>Tooltip</PageHeader>

- [Stories](#stories)
- [Accessibility](#accessibility)

## Overview

<Canvas>
  <Story name="Default">
    {args => (
      <Tooltip {...args}>
        <Tooltip.Element label="Here's my long tooltip" />
        <Tooltip.Trigger>
          <Button>Hover me to trigger the tooltip</Button>
        </Tooltip.Trigger>
      </Tooltip>
    )}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Stories

### Types of triggers

<Canvas>
  <Story name="Types of triggers">
    {args => {
      const RenderTooltip = ({trigger}) => (
        <Tooltip {...args}>
          <Tooltip.Element label="Here's tooltip" />
          <Tooltip.Trigger>{trigger}</Tooltip.Trigger>
        </Tooltip>
      );
      return (
        <Flex direction="column" style={{gap: '20px'}}>
          <div>
            <Text>Button component:</Text>
            <RenderTooltip trigger={<Button>Hover me</Button>} />
          </div>
          <div>
            <Text>Checkbox component:</Text>
            <RenderTooltip trigger={<Checkbox>Hover me</Checkbox>} />
          </div>
          <div>
            <Text>Button tag:</Text>
            <RenderTooltip
              trigger={<button>Hover me to trigger the tooltip</button>}
            />
          </div>
          <div>
            <Text>p tag:</Text>
            <RenderTooltip trigger={<p>Hover me to trigger the tooltip</p>} />
          </div>
          <div style={{width: '300px'}}>
            Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
            eiusmod tempor incididunt ut labore et dolore magna aliqua.{' '}
            <RenderTooltip trigger="Hover me to trigger the tooltip." /> Ut enim
            ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut
            aliquip ex ea commodo consequat.
          </div>
        </Flex>
      );
    }}
  </Story>
</Canvas>

### Placement

<Canvas>
  <Story name="Placement" args={{defaultOpen: true}}>
    {args => {
      return (
        <Flex direction="column" alignItems="center" style={{gap: '50px'}}>
          {TOOLTIP_PLACEMENTS.map(placement => (
            <Flex direction="column" style={{gap: '100px'}}>
              <Text>Position {placement}</Text>
              <Tooltip {...args} placement={placement}>
                <Tooltip.Element label="Here's a long tooltip Here's a long tooltip" />
                <Tooltip.Trigger>
                  <Button>Hover me</Button>
                </Tooltip.Trigger>
              </Tooltip>
            </Flex>
          ))}
          {TOOLTIP_PLACEMENTS.map(placement => (
            <Flex direction="column" style={{gap: '100px'}}>
              <Text>Position {placement}</Text>
              <Tooltip {...args} placement={placement}>
                <Tooltip.Element label="Here's a long tooltip Here's a long tooltip" />
                <Tooltip.Trigger>
                  <Button
                    icon={<Icon color="adaptive" type="report_flag_outlined" />}
                    iconOnly
                    variant="solid-light"
                  >
                    button
                  </Button>
                </Tooltip.Trigger>
              </Tooltip>
            </Flex>
          ))}
          {TOOLTIP_PLACEMENTS.map(placement => (
            <Flex direction="column" style={{gap: '100px'}}>
              <Text>Position {placement}</Text>
              <Tooltip {...args} placement={placement}>
                <Tooltip.Element label="tooltip" />
                <Tooltip.Trigger>
                  <Button>Hover me</Button>
                </Tooltip.Trigger>
              </Tooltip>
            </Flex>
          ))}
        </Flex>
      );
    }}
  </Story>
</Canvas>

### Flip

<Canvas>
  <Story name="Flip" args={{defaultOpen: true}}>
    {args => {
      return (
        <Flex direction="column" style={{gap: '50px'}}>
          {TOOLTIP_PLACEMENTS.map(placement => (
            <Flex direction="column" style={{gap: '100px'}}>
              <Text>Initial position: {placement}</Text>
              <Tooltip {...args} placement={placement}>
                <Tooltip.Element label="Here's a long tooltip Here's a long tooltip" />
                <Tooltip.Trigger>
                  <Button>Hover me</Button>
                </Tooltip.Trigger>
              </Tooltip>
            </Flex>
          ))}
          {TOOLTIP_PLACEMENTS.map(placement => (
            <Flex direction="column" style={{gap: '100px'}}>
              <Text>Initial position: {placement}</Text>
              <Tooltip {...args} placement={placement}>
                <Tooltip.Element label="Here's a long tooltip Here's a long tooltip" />
                <Tooltip.Trigger>
                  <Button
                    icon={<Icon color="adaptive" type="report_flag_outlined" />}
                    iconOnly
                    variant="solid-light"
                  >
                    button
                  </Button>
                </Tooltip.Trigger>
              </Tooltip>
            </Flex>
          ))}
        </Flex>
      );
    }}
  </Story>
</Canvas>

### Short tooltip

<Canvas>
  <Story name="Short tooltip" args={{defaultOpen: true}}>
    {args => {
      return (
        <div>
          {['top', 'top-start', 'top-end'].map(placement => (
            <Flex
              direction="column"
              style={{gap: '60px', marginBottom: '30px'}}
            >
              <Text>Position {placement}</Text>
              <Tooltip {...args} placement={placement}>
                <Tooltip.Element label="tooltip" />
                <Tooltip.Trigger>
                  <Button>Hover me</Button>
                </Tooltip.Trigger>
              </Tooltip>
            </Flex>
          ))}
          {['bottom', 'bottom-start', 'bottom-end'].map(placement => (
            <Flex
              direction="column"
              style={{gap: '10px', marginBottom: '90px'}}
            >
              <Text>Position {placement}</Text>
              <Tooltip {...args} placement={placement}>
                <Tooltip.Element label="tooltip" />
                <Tooltip.Trigger>
                  <Button>Hover me</Button>
                </Tooltip.Trigger>
              </Tooltip>
            </Flex>
          ))}
        </div>
      );
    }}
  </Story>
</Canvas>

### Sizes

<Canvas>
  <Story name="Sizes" args={{defaultOpen: true}}>
    {args => {
      return (
        <Flex direction="column" style={{gap: '50px'}}>
          <Flex justifyContent="center" style={{gap: '250px'}}>
            {['default', 'small'].map(size => (
              <Flex
                direction="column"
                alignItems="center"
                style={{gap: '60px'}}
              >
                <Text>Size {size}</Text>
                <Tooltip {...args} size={size}>
                  <Tooltip.Element label="Tooltip content" />
                  <Tooltip.Trigger>
                    <Button
                      icon={
                        <Icon color="adaptive" type="report_flag_outlined" />
                      }
                      iconOnly
                      variant="solid-light"
                    >
                      button
                    </Button>
                  </Tooltip.Trigger>
                </Tooltip>
              </Flex>
            ))}
          </Flex>
          <Flex
            justifyContent="center"
            direction="column"
            style={{gap: '60px'}}
            alignItems="center"
          >
            {['default', 'small'].map(size => (
              <Flex
                direction="column"
                style={{gap: '90px'}}
                alignItems="center"
              >
                <Text>Long tooltip, size {size}</Text>
                <Tooltip {...args} size={size}>
                  <Tooltip.Element
                    label="Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do
            eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim
            ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut
            aliquip ex ea commodo consequat."
                  />
                  <Tooltip.Trigger>
                    <Button
                      icon={
                        <Icon color="adaptive" type="report_flag_outlined" />
                      }
                      iconOnly
                      variant="solid-light"
                    >
                      button
                    </Button>
                  </Tooltip.Trigger>
                </Tooltip>
              </Flex>
            ))}
          </Flex>
        </Flex>
      );
    }}
  </Story>
</Canvas>
