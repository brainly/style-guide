import {ArgsTable, Meta, Story, Canvas} from '@storybook/addon-docs';
import {StoryVariant} from '../../_docs/utils';
import {Preview} from '../../_docs/blocks/';

import Flex from '../flex/Flex';
import Radio from './Radio';
import RadioGroup from './RadioGroup';

<Meta
  title="Components/Radio new"
  component={Radio}
  argTypes={{
    children: 'string',
  }}
/>

# Bubble

- [Stories](#stories)

## Overview

<Canvas>
  <Story name="Default">{args => <Radio {...args} />}</Story>
</Canvas>

<ArgsTable story="Default" />

## Stories

### With label

<Canvas>
  <Story name="With label">{args => <Radio {...args}>My radio</Radio>}</Story>
</Canvas>

### With external label

<Canvas>
  <Story name="With external label">
    {args => (
      <label htmlFor="myRadioID">
        <Flex
          style={{
            backgroundColor: 'lightblue',
            borderRadius: '8px',
            padding: '20px 40px',
          }}
        >
          Thix component is a label for radio
          <Radio {...args} id="myRadioID" customLabelId="myRadioID-label" />
        </Flex>
      </label>
    )}
  </Story>
</Canvas>

### With group

<Canvas>
  <Story name="With group">
    {args => {
      return (
        <RadioGroup label="Pet a dog" name="dog" value="Freja">
          <Radio value="Freja" />
          <Radio value="Gałgan" />
        </RadioGroup>
      );
    }}
  </Story>
</Canvas>
