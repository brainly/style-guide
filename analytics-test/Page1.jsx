import {Button} from 'brainly-style-guide';

export const Foo = () => (
  <div>
    <Button variant={variant} size="m">
      buttonText
    </Button>
    <Button role="link" size="xl">
      buttonText
    </Button>
  </div>
);
